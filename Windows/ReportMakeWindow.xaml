<Window x:Class="WellBeingDiary.Windows.ReportMakeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WellBeingDiary.Windows"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="Создание отчетов" Height="700" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Создание отчетов" FontSize="18" FontWeight="Bold" Margin="0 0 0 20"/>
        <StackPanel Grid.Row="1">
            <TextBlock Text="Тип отчета" Margin="0 5 0 5"/>
            <ComboBox Name="ComboBoxReportType" Height="25" Margin="0 0 0 10" SelectionChanged="ComboBoxReportType_SelectionChanged">
                <ComboBoxItem Content="Отчет по самочувствию"/>
                <ComboBoxItem Content="Отчет по давлению"/>
                <ComboBoxItem Content="Отчет по сну"/>
                <ComboBoxItem Content="Отчет по двигательной активности"/>
                <ComboBoxItem Content="Отчет по весу"/>
            </ComboBox>
            <StackPanel Orientation="Horizontal" Margin="0 5 0 10">
                <StackPanel>
                    <TextBlock Text="Начальная дата" Margin="0 5 0 5"/>
                    <DatePicker Name="DPStartDate" Height="25" Width="100" Margin="0 0 10 0"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Конечная дата" Margin="0 5 0 5"/>
                    <DatePicker Name="DPEndDate" Height="25" Width="100" Margin="0 0 10 0"/>
                </StackPanel>
            </StackPanel>
            <Button Name="GenerateReportButton" Content="Сгенерировать отчет" Click="GenerateReportButton_Click" Height="30"
        Background="White" Foreground="Black" BorderThickness="0"/>
        </StackPanel>
        <TabControl Grid.Row="2">
            <TabItem Header="Текстовый отчет">
                <TextBox Name="BoxReport" Height="300" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Background="WhiteSmoke" Padding="10"/>
            </TabItem>
            <TabItem Header="Представление графиком" Name="ChartTab">
                <lvc:CartesianChart Name="ReportChart" Series="{Binding ReportCollection}" LegendLocation="Right"/>
            </TabItem>
        </TabControl>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20 0 0">
            <Button Name="SaveReportButton" Content="Сохранить отчет" Click="SaveReportButton_Click" Width="100" Height="30" Margin="0 0 10 0" 
                Background="White" Foreground="Black" BorderThickness="0"/>
            <Button Name="CloseButton" Content="Закрыть" Click="CloseButton_Click" Width="100" Height="30"
                Background="White" Foreground="Black" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
